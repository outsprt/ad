# Зависимости
# Папка node_modules может весить сотни мегабайт. 
# Она всегда устанавливается локально командой npm install.
/node_modules

# Результаты сборки Next.js
# Эта папка генерируется при запуске 'npm run build'.
# В репозитории должен храниться только исходный код, а не результат сборки.
/.next/
/out/

# Файлы логов
# Логи могут содержать чувствительную информацию и не относятся к исходному коду.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Локальные переменные окружения
# В этих файлах хранятся секретные ключи (API, базы данных).
# Они НИКОГДА не должны попадать в публичный репозиторий.
.env
.env.local
.env.development.local
.env.production.local

# Системные файлы macOS и Windows
.DS_Store
Thumbs.db

# Файлы конфигурации IDE
.vscode/
.idea/

# Файлы отчетов о тестировании
/coverage
